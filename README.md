## To run the shopping-list app

### Start the Docker container with docker-compose

```bash
docker-compose up
```

### load swagger in the browser with the following url:

- http://localhost:8199/swagger-ui.html
- run the api for userId: 1